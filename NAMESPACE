import(Biostrings)

exportClasses(
    "BSgenome"
)

exportMethods(
    ## Generics defined elsewhere
    initialize, names, show, length, "[[", "$",
    ## New generics
    seqnames, mseqnames, unload
)

export(
    buildDataFiles, getSeq
)
