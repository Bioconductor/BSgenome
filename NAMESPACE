import(Biostrings)

exportClasses(
    "BSgenome"
)

exportMethods(
    ## Generics defined elsewhere
    initialize, seqnames, mseqnames, names, show, length, "[[", "$",
    ## New generics
    unload
)

export(
    buildDataFiles
)
