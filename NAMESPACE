import(methods)
import(Biobase) # just for the "biocReposList" function
import(IRanges)
import(Biostrings)

exportClasses(
    BSgenome,

    ## BSgenomeForge classes:
    BSgenomeDataPkgSeed
)

export(
    ## Constants:
    BUILTIN_MASKNAMES,

    ## Non-generic functions:
    available.genomes, getSeq,
    BSgenome, injectSNPs,
    available.SNPs,
    bsapply,

    ## Generic functions:
    organism, species, provider, providerVersion,
    releaseDate, releaseName, sourceUrl,
    SNPlocs_pkg, SNPcount, SNPlocs,
    seqnames, seqlengths, mseqnames,
    masknames,

    ## BSgenomeForge functions:
    getSeqSrcpaths,
    getSeqlengths,
    forgeSeqlengthsFile,
    forgeSeqFiles,
    forgeMasksFiles,
    forgeBSgenomeDataPkg
)

exportMethods(
    ## Methods for generics defined elsewhere:
    names, show, length, "[[", "$",

    ## Methods for generics defined in this package:
    organism, species, provider, providerVersion,
    releaseDate, releaseName, sourceUrl,
    SNPlocs_pkg, SNPcount, SNPlocs,
    seqnames, seqlengths, mseqnames,
    masknames,

    ## BSgenomeForge methods:
    forgeBSgenomeDataPkg
)

