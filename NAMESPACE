import(methods)
import(Biobase) # only for the 'biocReposList' function
import(Biostrings)

exportClasses(
    "BSgenome"
)

exportMethods(
    ## Generics defined elsewhere
    names, show, length, "[[", "$",
    ## New generics
    organism, species, provider, providerVersion,
    releaseDate, releaseName, sourceUrl,
    SNPlocs_pkgname,
    seqnames, mseqnames,
    unload
)

export(
    available.genomes, getSeq,
    BSgenome, injectSNPs,
    available.SNPs,

    ## BSgenomeForge functions
    forgeSeqFiles,
    forgeMaskFiles
)

