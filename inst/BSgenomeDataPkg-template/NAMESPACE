import(BSgenome)

### Don't export @BSGENOMEOBJNAME@ (the BSgenome object defined in this
### package): it is created and dynamically exported at load time (refer
### to R/zzz.R for the details).
#export(@BSGENOMEOBJNAME@)

